# ডেটাবেজ ভার্জিনিয়া → সিঙ্গাপুর

> **নতুন সেশনে এই ফাইলটা পড়েই কাজ শুরু করা যাবে।** আগের কথোপকথনের কিছু
> লাগবে না। যা যা মাপা ও যাচাই করা হয়েছে, সব নিচে লেখা আছে।

---

## কেন

সার্ভার সিঙ্গাপুরে (Render), ডেটাবেজ ভার্জিনিয়ায় (Neon)। মাঝের দূরত্বই
পুরো ধীরগতির কারণ। মাপা সংখ্যা (`/api/warmup/` — ঠিক একটি `SELECT 1`):

| | সময় |
|---|---|
| একটি প্রশ্ন, সংযোগ তৈরি থাকলে | **২১০–২৩৩ মি.সে.** |
| একটি প্রশ্ন, নতুন সংযোগ খুলতে হলে | **১,৭৭৫ মি.সে.** |
| ডেটাবেজ ছোঁয় না এমন ডাক | ০.১০ সে. |

সিঙ্গাপুরে এলে প্রতিটি প্রশ্ন ~২ মি.সে. — **প্রায় ১০০ গুণ দ্রুত**।

আশা করা যায়:

| | এখন | পরে |
|---|---|---|
| লগইন (৪ প্রশ্ন) | ~১.০৫ সে. | ~০.১৫ সে. |
| দারস খোলা (৩ প্রশ্ন) | ০.৭৫–২.৫ সে. | ~০.১২ সে. |
| পুরো পাতা (১০–১৫ প্রশ্ন) | ৩–৮ সে. | ~০.৫ সে. |

---

## নিরাপত্তার নিয়ম — এগুলো ভাঙা যাবে না

1. **পুরনো ডেটাবেজে কখনো লেখা হবে না — শুধু পড়া।** তাই সেটি সবসময় অক্ষত।
2. **সারি গুনে না মিললে এগোনো যাবে না।** পরিচালককে Render বদলাতে বলার
   আগে প্রতিটি টেবিলের সংখ্যা হুবহু মিলতে হবে।
3. **ক্লাস চলাকালীন নয়।** ব্যাকআপের পর ও ঠিকানা বদলের আগে কেউ কিছু লিখলে
   সেটুকু হারাবে। এটাই একমাত্র সত্যিকারের ঝুঁকি।
4. **`backup.json` মুছবেন না** — ওটাই নিরাপত্তা জাল।
5. নতুন ডেটাবেজে `migrate`-এর আগে বা পরে **অ্যাপে ঢুকে কিছু বানাবেন না** —
   তাতে ডুপ্লিকেট হবে।

**ছবি/PDF/রসিদ নড়বে না** — সেগুলো Cloudinary-তে, ডেটাবেজে নয়।

---

## কে কী করবে

**পরিচালক (৩টি কাজ, ~১০ মিনিট)**

1. neon.tech → New Project → Region **Singapore (ap-southeast-1)**
2. `backend/.env`-এ দুই লাইন (চ্যাটে পাঠাবেন না):
   ```
   OLD_DATABASE_URL=Render ড্যাশবোর্ড থেকে
   NEW_DATABASE_URL=Neon সিঙ্গাপুর থেকে
   ```
3. শেষে Render → Environment → `DATABASE_URL` = নতুনটা → **Manual Deploy**

**Claude (বাকিটা)** — ব্যাকআপ, টেবিল তৈরি, তথ্য বসানো, গুনে মেলানো,
ভেতরের লেখা যাচাই।

---

## ধাপে ধাপে

PowerShell, `backend` ফোল্ডার থেকে।

### ১. পুরনো থেকে ব্যাকআপ

```powershell
$env:DATABASE_URL = "<OLD>"
python manage.py dumpdata --natural-foreign --natural-primary `
  --exclude contenttypes --exclude auth.permission `
  --exclude sessions --exclude admin.logentry `
  --indent 1 -o backup.json
```

### ২. আগের গণনা লিখে রাখা

```powershell
python manage.py shell -c "from django.apps import apps; import json; d={m.__name__: m.objects.count() for m in apps.get_app_config('core').get_models()}; open('_before.json','w').write(json.dumps(d)); print(sum(d.values()),'সারি')"
```

### ৩. নতুনে টেবিল ও তথ্য

```powershell
$env:DATABASE_URL = "<NEW>"
python manage.py migrate
python manage.py loaddata backup.json
```

### ৪. গুনে মেলানো — **এখানে না মিললে থামুন**

```powershell
python manage.py shell -c "from django.apps import apps; import json; b=json.load(open('_before.json')); bad=[(m.__name__,b.get(m.__name__,0),m.objects.count()) for m in apps.get_app_config('core').get_models() if b.get(m.__name__,0)!=m.objects.count()]; print('সব মিলেছে' if not bad else bad)"
```

### ৫. ভেতরের লেখাও যাচাই

```powershell
python manage.py shell -c "from core import models as M; l=M.Lesson.objects.filter(title_ar='الإخلاص').first(); ar=' '.join(s.arabic or '' for st in l.steps.all() for s in [getattr(st,'slide',None)] if s); print('ধাপ',l.steps.count()); print('আয়াত',('قُلْ هُوَ ٱللَّهُ أَحَدٌ' in ar)); print('আয়াত-চিহ্ন','۝١' in ar); print('অগ্রগতি',M.LessonProgress.objects.count()); print('টপিক',M.LectureTopic.objects.count())"
```

### ৬. পরিচালক Render বদলাবেন

তারপর বাইরে থেকে সময় মেপে দেখা:

```bash
for i in 1 2 3 4 5; do curl -s -o /dev/null -w "%{time_total}\n" -X POST \
  -H "Content-Type: application/json" -d '{"username":"__p__","password":"__p__"}' \
  https://api.tarbiyatulquran.org/api/auth/login ; done
```
**~১.০৫ সে. থেকে নেমে ~০.১৫ সে. হওয়ার কথা।**

---

## ফেরার পথ

Render-এ পুরনো `DATABASE_URL` ফিরিয়ে দিন → Manual Deploy। ২ মিনিটে আগের
অবস্থা। পুরনো ডেটাবেজে কিছুই বদলানো হয়নি।

---

## মহড়া — পদ্ধতিটা পরখ করা হয়েছে

স্থানীয় ডেটাবেজে পুরো যাওয়া-আসা চালিয়ে দেখা হয়েছে: **৪১৪ সারি, প্রতিটি
টেবিল হুবহু মিলেছে।** আয়াত, আয়াত-চিহ্ন, উস্তাদের স্ক্রিপ্ট, বাংলা
নির্দেশনা, শিক্ষার্থীর অগ্রগতি, টপিক→হেডিং সংযোগ, কোর্স→উস্তাদ সংযোগ —
সব অক্ষত ছিল।

---

## জানা ফাঁদ

- **নতুন ডেটাবেজে data-migration গুলো কিছুই বসাবে না** — ০০৩৬/০০৪১/০০৪৩ সব
  কোর্স বা দারস ১ খুঁজে না পেলে চুপচাপ থেমে যায়। ফাঁকা ডেটাবেজে তাই কিছুই
  বসে না, আর `loaddata` সব নিয়ে আসে। মহড়ায় এটাই দেখা গেছে (৮৩টি মাইগ্রেশন
  প্রয়োগ, তারপর ৪১৪ অবজেক্ট লোড, গণনা মিলেছে)।
- **`pg_dump` এই মেশিনে নেই** — তাই Django-র `dumpdata`/`loaddata`।
- **সরানোর পর বাড়তি অপ্টিমাইজেশন যোগ করা যাবে না** সাথে সাথেই। আগে
  স্থিতিশীল হোক, তারপর একটা একটা করে।

## ⚠️ আগের একটি শিক্ষা

২০২৬-০৮-২৭: "ডেটাবেজ আগেভাগে জাগানো" নামে প্রতি পাতা-লোডে একটি বাড়তি
ডাক যোগ করা হয়েছিল। ক্লাস চলাকালে অ্যাপ অচল হয়ে যায় — সম্ভবত প্রতিটি নতুন
সংযোগে ১.৮ সে. লাগায় গুনিকর্নের ৮টি থ্রেড আটকে যাচ্ছিল। ফিরিয়ে নিয়ে
(`8853843`) ঠিক হয়। **ডেটাবেজ দূরে থাকতে বাড়তি ডাক যোগ করা নিরাপদ নয়।**
সরানোর পর ওই ব্যবস্থাটি আবার ভেবে দেখা যেতে পারে — তখন আর দরকারই হয়তো
থাকবে না।

---

## ব্যবহারের সীমা

২০২৬-০৮-২৭-এ সাপ্তাহিক সীমার ৮৭% ব্যবহৃত ছিল, রিসেট ৩ দিন পর। খরচের
প্রধান কারণ — খুব লম্বা সেশন (>১৫০k কনটেক্সট)। **তাই এই কাজটি নতুন,
পরিষ্কার সেশনে করা হচ্ছে**, আর একসাথে অন্য কোনো কাজ নেওয়া হবে না।
