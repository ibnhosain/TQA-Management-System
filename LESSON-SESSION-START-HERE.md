# 📗 দারস লেখার সেশন — এখান থেকে শুরু

> **নতুন ট্যাবে প্রথমেই এই ফাইলটা পড়তে বলুন।** আগের কোনো কথোপকথন
> লাগবে না — যা যা জানা দরকার সব এখানে আছে।

---

## নতুন ট্যাবে যা লিখবেন

```
LESSON-SESSION-START-HERE.md পড়ে নাও। আজ [সূরা/দারসের নাম] লিখব।
```

ব্যস। এটুকুই।

---

## ⚠️ এই সেশনে কেবল দারস — আর কিছু নয়

অ্যাপের বাগ, ডেটাবেজ, লগইন, জুম — এগুলো **অন্য ট্যাবে**। এখানে হাত
দেওয়া হবে কেবল দুটি ফাইলে:

| ফাইল | কী থাকে |
|---|---|
| `backend/core/sample_lessons.py` | সব দারসের লেখা |
| `backend/core/tests_lesson_scripts.py` | নিয়মগুলোর পাহারা |
| `backend/core/migrations/00XX_*.py` | দারসটি টপিকে বসানোর কাজ |

মেশানো সেশন লম্বা হয়, আর লম্বা সেশনই সাপ্তাহিক সীমার সবচেয়ে বড় খরচ।

---

## এখন পর্যন্ত যা লেখা হয়েছে

| চাবি | দারস | কোথায় বসে |
|---|---|---|
| `ikhlas` | সূরা আল-ইখলাস | হিফজ কোর্স |
| `qaida` … `qaida5` | কায়দা দারস ১-৫ | Quran/Qirat হেডিং, নম্বর ধরে |
| `kawthar` | সূরা আল-কাউসার | Memorized Surah হেডিং, নাম ধরে |
| `nas` | সূরা আন-নাস | Memorized Surah হেডিং, নাম ধরে |

সব `SAMPLES` ডিকশনারিতে নিবন্ধিত — নতুনটিও সেখানে যোগ করতে হবে।

---

## একটি নতুন দারস কীভাবে যোগ হয় — ৫ ধাপ

**১.** `sample_lessons.py`-তে নতুন ডিকশনারি লিখুন (যেমন `FALAQ = {...}`),
আর `SAMPLES`-এ চাবি যোগ করুন।

**২.** `tests_lesson_scripts.py`-এর `ALL` টুপলে নতুন দারসটি যোগ করুন।
⚠️ **এটা ভুলে গেলে দারসটি সব নিয়মের বাইরে থেকে যায়** — কোনো পরীক্ষাই
তাকে দেখবে না।

**৩.** নতুন মাইগ্রেশন লিখুন যা দারসটি ঠিক টপিকে বসায়। নমুনা:
`core/migrations/0047_nas_lesson.py` — নাম ধরে খোঁজে।
কায়দার মতো নম্বরওয়ালা হলে `topic_for_number()` ব্যবহার করুন।

**৪.** `0046_seed_any_missing_lesson.py`-এ নতুন দারসটিও যোগ করুন — যাতে
ভবিষ্যতে ডেটাবেজ সরালে এটিও আপনাআপনি বসে যায়।

**৫.** যাচাই করুন (নিচে)।

---

## যাচাই — পুশ করার আগে সবসময়

```bash
cd backend
python manage.py test                  # সব পরীক্ষা
rm -f db.sqlite3 && python manage.py migrate   # শূন্য থেকে পুরো চেইন
```

পরীক্ষা লাল থাকলে **কখনো পুশ নয়**।

---

## লেখার নিয়ম

পুরো নিয়ম আছে **`LESSON-SCRIPT-MASTER-PROMPT.md`**-এ — সেটি পড়ে নিন।
সবচেয়ে জরুরি কয়েকটি এখানে মনে করিয়ে দিচ্ছি:

- উস্তাদ যা **মুখে বলবেন** তা ইংরেজিতে, প্রচলিত মুখের ইংরেজি
  (`don't`, `let's`, `it's` — `do not` নয়)
- **বন্ধনীর ভেতরের নির্দেশনা বাংলায়** — `[অপেক্ষা করুন]`
- `does` ও `note` **সবসময় বাংলায়** — এগুলো উস্তাদের জন্য
- **আরবি কখনো ইংরেজি অক্ষরে নয়** — বলার জায়গায় `DOTS` (…………)
- বাক্য **১৪ শব্দের বেশি নয়**
- ভুল হলে কখনো **"Wrong"** নয়
- গায়ে লেখা মিনিট আর ধাপের সেকেন্ডের যোগফল **মিলতে হবে**

---

## ⚠️ যে ভুলগুলো আগে হয়েছে — আবার যেন না হয়

**১. আয়াতের ভেতরের দাবি যাচাই না করে লেখা।**
আন-নাসে লিখেছিলাম ٱلنَّاسِ শব্দটি ছয় আয়াতেরই শেষে আছে। **ভুল** — ৪ নম্বর
আয়াত শেষ হয় ٱلْخَنَّاسِ দিয়ে, শব্দটি আছে পাঁচবার। শিশুকে কুরআন সম্পর্কে
ভুল শেখানো যায় না। **প্রতিটি দাবি আয়াতের পাঠে গুনে মিলিয়ে নিন।**

**২. দারস ভুল টপিকে বসা।**
আগে `order` ধরে বসানো হতো — দারস ২ ও ৩ গিয়ে বসেছিল ৯ ও ১০ নম্বরে।
এখন টপিকের **নামের ভেতরের নম্বর** পড়া হয় (`topic_number()`)। আর একবার
ঢিলে নিয়মে "Memorized Hadith 4"-ও মিলে গিয়েছিল — **মিলের নিয়ম আঁটসাঁট
রাখুন।**

**৩. চলে যাওয়া মাইগ্রেশন আর কখনো চলে না।**
কোড শুধরালেও Django সেটিকে "হয়ে গেছে" ধরে রাখে। ভুল বসে গেলে **নতুন
নম্বরের মেরামতি মাইগ্রেশন** লিখতে হয় (`0042`, `0044` দেখুন)।

**৪. পরিচালকের নিজের লেখা কখনো মোছা যাবে না।**
যে টপিকে আগে থেকেই স্ক্রিপ্ট আছে, বা টগলে নিজের লেখা আছে — সেখানে হাত
দেওয়া নিষেধ। প্রতিটি মাইগ্রেশনে এই পাহারা আছে, নতুনটিতেও রাখুন।

**৫. এক দারস অন্যটার কপি নয়।**
ছক এক থাকবে (শোনো → বলো → জোড়া), কিন্তু শুরু, খেলা, শেষ — সব আলাদা।
কাউসার শুরু হয় গল্প দিয়ে, আন-নাস ধাঁধা দিয়ে। **নতুনটির নিজস্ব চাবি
খুঁজে বের করুন** — সূরাটির ভেতরেই সেটা থাকে।

**৬. খুব লম্বা bash কমান্ড কাজ করে না** (`ENAMETOOLONG`)। বড় লেখা
আলাদা ফাইলে লিখে তারপর জুড়ে দিন।

---

## দারসটি সত্যিই বসেছে কিনা দেখা

ডিপ্লয়ের পর ব্রাউজারে খুলুন:

```
https://api.tarbiyatulquran.org/api/version/
```

শেষ মাইগ্রেশনের নাম দেখাবে। আপনার নতুন মাইগ্রেশনের নাম দেখালে কাজ
হয়েছে; পুরনো নাম দেখালে Render-এ ডিপ্লয় হয়নি।
